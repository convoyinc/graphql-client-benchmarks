query partial25Query($convId: ID!) {
  messages(convId: $convId) {
    from
    originalArrivalTime
    messageType
    emotions {
      key
      users {
        id
      }
    }
    content
    imDisplayName
    __typename
    id
  }
}
query partial16Query($convId: ID!) {
  messages(convId: $convId) {
    emotions {
      key
      users {
        id
      }
    }
    from
    messageType
    content
    imDisplayName
    originalArrivalTime
    __typename
    id
  }
}
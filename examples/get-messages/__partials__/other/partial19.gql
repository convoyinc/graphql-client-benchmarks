query partial19Query($convId: ID!) {
  messages(convId: $convId) {
    messageType
    from
    emotions {
      key
      users {
        id
      }
    }
    imDisplayName
    originalArrivalTime
    content
    __typename
    id
  }
}
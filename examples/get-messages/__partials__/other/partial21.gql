query partial21Query($convId: ID!) {
  messages(convId: $convId) {
    content
    originalArrivalTime
    emotions {
      key
      users {
        id
      }
    }
    from
    imDisplayName
    messageType
    __typename
    id
  }
}
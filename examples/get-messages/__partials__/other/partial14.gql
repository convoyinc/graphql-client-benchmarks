query partial14Query($convId: ID!) {
  messages(convId: $convId) {
    content
    from
    messageType
    emotions {
      key
      users {
        id
      }
    }
    imDisplayName
    originalArrivalTime
    __typename
    id
  }
}
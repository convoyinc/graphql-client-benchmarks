query partial13Query($convId: ID!) {
  messages(convId: $convId) {
    originalArrivalTime
    content
    from
    emotions {
      key
      users {
        id
      }
    }
    messageType
    __typename
    id
  }
}
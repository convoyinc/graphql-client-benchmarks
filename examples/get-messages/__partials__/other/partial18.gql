query partial18Query($convId: ID!) {
  messages(convId: $convId) {
    messageType
    content
    emotions {
      key
      users {
        id
      }
    }
    imDisplayName
    from
    originalArrivalTime
    __typename
    id
  }
}